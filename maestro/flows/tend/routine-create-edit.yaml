# Routine create/edit E2E flow
#
# NOTE: Settings now has a live "Routines" section with a "+ Add Routine"
# entry (see maestro/flows/settings/settings-routines.yaml) — this flow can be
# updated to navigate via Settings → Routines → + Add Routine. The Tend tab
# "+ Add Routine" entry point is still wired in a separate issue. Until that
# Tend work lands, the assertions below describe the intended Tend behavior and
# will be activated once the Tend calling screen exists.
#
# To validate in the meantime you can deep-link directly to the create screen:
#   appId: com.haven.app
#   launchArguments: { initialUrl: "haven://routine/create" }
#
# See also: maestro/flows/tend/routine-complete.yaml — the complete (execution)
# flow for an existing routine.

appId: com.haven.app

---

# STUB — update navigation steps once Tend +Add Routine button is wired

- launchApp

# Navigate to Tend tab (home)
- tapOn:
    id: "tend-tab"

# --- Create a new Routine ---
# TODO: tap the "+ Add Routine" button once it exists on Tend
# - tapOn:
#     text: "+ Add Routine"

# Fill in the routine name
# - tapOn:
#     id: "routine-name-input"
# - inputText: "Morning Flow"

# Select a time block
# - tapOn:
#     id: "routine-time-block-Morning"

# Add first item
# - tapOn:
#     id: "routine-add-item-button"
# - tapOn:
#     id: "routine-item-0-name-input"
# - inputText: "Breakfast"
# - tapOn:
#     id: "routine-item-0-entry-type-1"   # Food

# Add second item
# - tapOn:
#     id: "routine-add-item-button"
# - tapOn:
#     id: "routine-item-1-name-input"
# - inputText: "Morning walk"
# - tapOn:
#     id: "routine-item-1-entry-type-6"   # Activity

# Save
# - tapOn:
#     id: "routine-save-button"

# Verify routine appears in Tend (once Tend integration is complete)
# - assertVisible:
#     text: "Morning Flow"

# --- Edit the Routine ---
# TODO: tap the routine card on Tend to navigate to edit screen
# - tapOn:
#     text: "Morning Flow"

# Change the name
# - clearText:
#     id: "routine-edit-name-input"
# - inputText: "Updated Morning Flow"

# Save
# - tapOn:
#     id: "routine-edit-save-button"

# Verify updated name appears in Tend
# - assertVisible:
#     text: "Updated Morning Flow"
