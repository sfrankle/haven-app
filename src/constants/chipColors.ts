import { colors } from './theme/tokens';

export const CHIP_COLORS: string[] = [
  colors.chipRose,
  colors.chipPeach,
  colors.chipButter,
  colors.chipSage,
  colors.chipMint,
  colors.chipAqua,
  colors.chipPeriwinkle,
  colors.chipLavender,
];
